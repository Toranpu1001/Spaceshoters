/// @description Insert description here
// You can write your code in this editor
show_debug_message(image_alpha)
	if(first_fire == false)
	{
		image_alpha = lerp(image_alpha, 1, .006);
	}

if (change > 1.1 || change < .9)
{
	change_scale *= -1
}

change += change_scale;

































